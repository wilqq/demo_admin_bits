<%= form_for [:admin, @product] do |f| %> 

  <%= f.label :name %>
  <%= f.text_field :name %> 

  <%= f.label :description %>
  <%= f.text_field :description %> 

  <%= f.label :price %>
  <%= f.text_field :price %> 

  <%= f.submit class: 'button success', id: 'submit' %>
<% end %>
